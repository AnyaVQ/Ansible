---
- hosts: Routers
  gather_facts: no


  tasks:
  - name: REGISTER VTY CONFIG ON DEVICE
    ios_command:
      commands:
        - show version
        - show ip int brief
    register: vty

  - debug: var=vty.stdout_lines

